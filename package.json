{
  "name": "consequence",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy:pages": "git subtree push -f --prefix public origin gh-pages"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
